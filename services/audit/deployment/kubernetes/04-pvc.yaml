apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: audit-exports-pvc
  namespace: anumate-audit
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  storageClassName: fast-ssd
