[tool.poetry]
name = "anumate-capability-tokens"
version = "0.1.0"
description = "JWT-like capability tokens for the Anumate platform."
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.11"
PyJWT = "^2.4.0"
anumate-crypto = {path = "../anumate-crypto", develop = true}

[tool.poetry.dev-dependencies]
pytest = "^7.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
